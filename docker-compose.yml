version: '3.7'

services: 
  backend:
      container_name: peloton-backend
      build: 
        context: .
        dockerfile: Dockerfile
      command: uvicorn main:app --host 0.0.0.0 --port 3001
      ports:
      - "3001:3001"
      volumes: 
        - ./app:/app/:cached